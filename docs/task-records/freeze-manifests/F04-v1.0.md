# Freeze Manifest：F04 v1.0

| 字段 | 内容 |
|---|---|
| 任务包 | F04 库存与主数据维护 |
| 冻结版本 | v1.0 |
| 当前任务包路径 | `docs/task-packages/F04/task.md` |
| 只读任务快照 | `docs/task-packages/F04/frozen/v1.0/task.md` |
| 实施期交付物（冻结时未产出） | `docs/task-packages/F04/evidence-manifest.md`（D06）、`docs/task-packages/F04/` 下逐 tool 可写字段白名单清单（D03）/对齐核对记录（D04）/开发自检记录（D05）/独立验收记录（D08）；MCP server 实现代码 `server/`（D02，仓库根，追加 10 个写/只出 plan tool） |
| 上游基线 | 《开源后端 Agent 化接入规范》2026-09-07 定稿版；《ERPNext-MCP 改造 PRD》2026-09-12-r1；《MCP 改造任务包总则》v1.1（升版 2026-09-15，依据 CHG-20260915-002）；《MCP 改造封闭任务包模板》v1.0；《MCP 改造任务包登记表》v1.0 字段结构基线；《实施区与验收区边界》v1.1（2026-09-15，依据 CHG-20260915-001）；D02 tool-contract v1.0（已通过，§3 #6–#12、§6 #23–#25 逐 tool 冻结口径，F04 直接依据）；D01 common-contract v1.0（已通过）；E01 v1.0（已封存）；E02 implementation-contract v1.0（已封存）；F01 task.md 与 `server/` 骨架 v1.0（已通过，直接上游）；C01a object-scope v1.0（已封存）；B01 interface-facts v1.0（已封存，主数据）；B04 interface-facts v1.0（已封存，库存）；C01b v1.0（已封存，仅版本标识，Implementer 不可读正文/断言） |
| 变更记录 | 首次冻结（2026-09-17），规划中→草拟→待评审→已冻结；冻结前两项待决裁定 + 三项评审修正（见「评审结论」） |
| Owner | gjg |
| Implementer | Claude（F04 独立实施上下文，与 Acceptor 隔离；不得读取 C01b 冻结任务集正文/断言） |
| Implementation Reviewer | gjg（评审输入与实施方案，不得读取 C01b 冻结任务集正文/隐藏断言） |
| Acceptance Reviewer | 不适用（F04 为实施任务；正式验收由 G01 集成验收承担） |
| Acceptor | gjg（档位 3 独立验收，与 Implementer 隔离；不读 C01b 题，以公开契约 + 后端终态/接口实测验收） |
| 冻结日期 | 2026-09-17 |
| 评审结论 | gjg 复核通过，无阻断问题，批准 F04 v1.0 冻结。评审中在两项待决裁定（① 登记表 F04 行上游依赖补入 F01；② 可写字段白名单降为工作项 W05）基础上另修正三项：③ §6.2/§16 backend.js 扩展清单改为「POST/PUT + `frappe.client.submit`（confirm #24）」，删除 F04 无需的 DELETE（草稿删除回滚归管理员运维、mcp-service 无 delete DocPerm）与 `frappe.client.save`（F02 cancel 路径）；④ §9.2 补明 #23 全自动档免确认仅限 L3、客户端不支持 `elicitation` 时全量 fail-closed（E01 §2.2）；⑤ §16「B03 F9」改引「B04 §4 回滚路径汇总已转引 B03 F9」。尚未实施或验收。 |
| 当前 task.md SHA-256 | `993d108fdebd77c9bf6ddc65611797885655671b6c931651924e96416b1ebc50` |
| 快照 task.md SHA-256 | `993d108fdebd77c9bf6ddc65611797885655671b6c931651924e96416b1ebc50` |

冻结范围包括 F04 v1.0 的目标、非目标、权威输入、授权范围（可读/写入集/系统权限）、禁止事项、工作项（W01—W10）、输入输出契约（10 个写/只出 plan tool 实现契约汇总 §9.1 + E01 确认/白名单/权限挂接 §9.2 + E02 机制挂接 §9.3 + 后端写调用落地 §9.4）、不变量、交付物（D01—D08）、自检方法（S01—S12）、完成定义及证据计划。实施期间不得降低或改写冻结语义。

只读快照由冻结时当前 `task.md` 生成，正文内容逐字符一致（当前文件与快照均采用 LF 且保留末尾换行，故当前哈希与快照哈希相同）。恢复或比对时以快照内容及其哈希为准。

`evidence-manifest.md`（D06）、逐 tool 可写字段白名单清单（D03）、对齐核对记录（D04）、开发自检记录（D05）、独立验收记录（D08）与 MCP server 实现代码 `server/`（D02，追加 10 个写/只出 plan tool）为实施期产出（总则 §4.3：契约/实现交付物是本包产出而非冻结前置条件），冻结时未产出、不纳入冻结快照；实施中形成后按冻结规则追加，最终由 Acceptor 独立验收时核对。

实施期间当前 `task.md` 可按冻结规则追加状态及实际证据，但不得覆盖 `frozen/v1.0/` 快照。

> 注：「当前 task.md SHA-256」记录的是**冻结时刻**的当前文件哈希（当时与快照哈希相同）。实施期按上述规则追加状态与证据后，当前文件哈希会与冻结时刻不同，本清单不再随之更新；恢复与比对一律以 `frozen/v1.0/` 快照及其哈希为准。

## 权威输入版本清单

1. 《开源后端 Agent 化接入规范》2026-09-07 定稿版；
2. 《ERPNext-MCP 改造 PRD》`2026-09-12-r1`；
3. D02 业务 tool 契约 `tool-contract.md` v1.0（已通过，§3 主数据 #6–#12、§6 库存 #23–#25 逐 tool 冻结口径，F04 直接依据）；
4. D01 公共契约与错误模型 `common-contract.md` v1.0（已通过，统一基座）；
5. 《MCP 改造任务包总则》v1.1（升版 2026-09-15，依据 CHG-20260915-002）；
6. 《MCP 改造封闭任务包模板》v1.0（以对应 Freeze Manifest 为准）；
7. 《MCP 改造任务包登记表》v1.0 字段结构基线（任务行受控更新）；
8. 《实施区与验收区边界》v1.1（升版 2026-09-15，依据 CHG-20260915-001）；
9. E01 权限矩阵/两张允许清单/正式账号与角色/确认与 fail-closed/越权验证 v1.0（已封存：`permission-matrix.md`、`allowlist.md`、`roles.md`、`confirmation-failclosed.md`、`authorization-verification.md`）；
10. E02 实现契约 `implementation-contract.md`（+ `lib/` 8 模块）v1.0（已封存，幂等/前置/事后/批次台账接口 + #26 查询语义）；
11. F01 task.md 与 `server/` 骨架 v1.0（已通过，直接上游：`server/` 骨架 + 迁入 E02 `lib/` 8 模块 + 6 读 tool + 白名单层）；
12. C01a 对象范围边界名单 `object-scope.md` v1.0（已封存，12 操作 + 9 引用）；
13. B01 接口事实记录 `interface-facts.md` v1.0（已封存，主数据：#6–#12 重名/版本/区间/级联删除）；
14. B04 接口事实记录 `interface-facts.md` v1.0（已封存，库存：#23–#25 submit 端点/库存校验时机/覆盖基线）；
15. C01b 冻结任务集 v1.0（已封存，仅版本标识；Implementer 不可读正文/初始数据/精确断言/评分细节）。

## 上下游依赖

- 上游：F01（已通过，直接上游，复用 `server/` 骨架 + 白名单层）；D02（已通过，§3 #6–#12、§6 #23–#25 直接依据）；D01（已通过，公共契约与错误模型）；E01（已封存，权限/允许清单/正式账号/确认与 fail-closed）；E02（已封存，幂等/前置/事后/批次台账机制 + #26 查询逻辑）；C01b（已封存，仅版本标识）；C01a（已封存，object-scope 名单为权威输入）；B01、B04（已封存，接口事实）。
- 下游：F02（销售与采购业务闭环，直接下游）；G01（完整集成验收）。
- 串行路径：F01 → F04 → F02（总则 §13 依赖规则）。F01 是第一环（6 读 tool + `server/` 骨架），F04 是第二环（#6–#12 主数据 + #23–#25 库存，共 10 个写/只出 plan tool），F02 是第三环（#13–#22 销售与采购写 tool）。F04 复用 F01 的 `server/` 骨架（入口/注册表只增不改）与白名单层。
- 门槛：F01/D02/D01/E01/E02/C01b/C01a/B01/B04 均已通过或封存（F04 冻结前置已解除）；F04 通过后方可启动 F02（直接下游）与 G01 集成验收。

## 完成定义（冻结口径）

见 `docs/task-packages/F04/task.md` 第 14 节，逐条可判定；冻结时全部为未完成态，仅在实施与档位 3 独立验收后逐项勾选。冻结后不得擅自改变完成定义。

## 已知限制

- 受题污染：单一自然人 gjg 兼任 Owner / Implementation Reviewer / Acceptor；F04 为档位 3 独立验收，Acceptor（gjg）不读 C01b 题（F04 验收以公开契约 + 后端终态/接口实测为准，无需隐藏断言；正式隐藏断言验收由 G01 承担）；
- Item→Item Price 级联删除（B01 F4）：后端删除被 Item Price 引用的 Item 时不拒绝、级联删除 Item Price，F04 的 Item 删除/回滚（#10 create 回滚）须自建引用前置检查；已被交易单据引用的 Item 删除不级联、转管理员处置（B04 §4 转引 B03 F9）；
- #23/#24 server 前置断言责任（B04 F5）：源仓可用量不足（#23）与源/目同仓（#24）后端不原生强制，F04 必须 server 侧前置断言；
- #25 只出 plan 边界：`stock_reconciliation_plan` 不提交、不覆盖库存基线；盘点实际提交所需的估值率/opening-entry 门槛（B04 F7/F8）属 G01 或后续盘点提交范围；
- 档位 3 封存口径：F04 为档位 3（完整），封存与下游影响记录 + Freeze Manifest 强制单独进行（总则 §7.1）；
- F04 尚未进入实施中。

## 验收责任人

- Implementation Reviewer：gjg（评审输入与实施方案，不得读取 C01b 冻结任务集正文/隐藏断言）；
- Acceptance Reviewer：不适用（F04 为实施任务；正式验收由 G01 集成验收承担）；
- Acceptor：gjg（档位 3 独立验收，与 Implementer 隔离；不读 C01b 题，以公开契约 + 后端终态/接口实测验收）。

## 封存记录（档位 3 强制，2026-09-17）

- 封存结论：**已封存**。档位 3 独立验收通过（Acceptor gjg，独立于 Implementer，`acceptance-record.md` D08），10 写/只出 plan tool 经逐 tool 静态核对 + 自检独立重跑（S02–S17 118 项）+ 真实后端全接口实测全部达标；首轮两项阻断（index.js 文案、后端实测覆盖）与复验 #25 默认值缺陷均已整改闭环；未读 C01b 冻结任务集正文/断言、未实现 #13–#22、未改 F01 既有 6 读 tool 与 E02 `lib/` 8 模块。
- 封存后为 F02（直接下游）提供 10 写/只出 plan tool 与复用 `server/` 骨架（入口/注册表只增不改、串行追加 #13–#22、以 mcp-service 为后端调用方、挂接 E01 确认/E02 机制）；G01 完整集成验收以 E/F 全部完成（含 F04）为前置。下游影响记录见 `task.md` §18。

## 实施期产出哈希（封存时登记）

实施期产出（本清单在冻结时未产出，见第 9 行）封存时登记的 SHA-256：

| 产出 | 路径 | SHA-256 |
|---|---|---|
| 独立验收记录（D08） | `docs/task-packages/F04/acceptance-record.md` | `7862faf000d1739470ba7af5a004ba9290bb86f4ca73786e39b427bf90be6081` |
| 开发自检记录（D05） | `docs/task-packages/F04/dev-selftest.md` | `dd63d22ffd768f3450a3afd5fd200b9c79928417f67da9cdcd9fdc791102a135` |
| Evidence Manifest（D06） | `docs/task-packages/F04/evidence-manifest.md` | `74698ef00c7ac2396ce0787d87113091b73d5dd8ec9254ac9ad27eee1edefa76` |
| 可写字段白名单（D03） | `docs/task-packages/F04/writable-fields-whitelist.md` | `5c5dcda03a435d733e57bd596a59be98b3f954e9d5da75e7483c8fd6f4df3cd3` |
| 对齐核对记录（D04） | `docs/task-packages/F04/alignment-check.md` | `185c7147c8b0499904fdb68e41a55acc744abed9136a55f71c61ffcda2396b17` |
| MCP server 实现（D02，聚合） | `server/`（仓库根，追加 10 写/只出 plan tool） | `971a4a35bcc6703a51f14769c8dec266d155b67ff226203dcefa73ce7f4f9e8d` |

> 说明：`server/` 聚合哈希为 `find server -type f -not -path '*/node_modules/*' | sort | xargs sha256sum | sha256sum` 之结果（文件清单排序后逐文件哈希再聚合），用于整体核对实现代码未被改动；单文件哈希以仓库内 `sha256sum` 逐文件为准。
