# Freeze Manifest

每个冻结文档使用一个独立清单，文件名格式为 `<文档名>-<版本>.md`。

清单至少记录：

- 文档名称、冻结版本和路径；
- 全部上游基线及版本；
- Reviewer、决策人和冻结日期；
- 评审结论及评审记录位置；
- 冻结文件的 SHA-256 哈希；
- 已知限制与尚未完成的下游门槛。

哈希必须针对最终冻结文件计算，不得把哈希写回被哈希文件本身。
