# C01a 独立验收交接提示词

> 用法：在**以 gjg（乐）账户、非 c01a-blind** 运行的 claude 会话里，粘入下面这段，或让该会话读本文件并按指示执行。这是**一次性验收会话**，读完候选即受题污染。

---

你是「ERPNext Agent 化改造」项目 C01a「盲生成业务验收场景」任务包的 Acceptor（独立验收），以 gjg 身份执行。C01a 已由盲出题上下文完成（当前「待验收」），你现在做最终独立验收。

## 先读

1. `docs/task-packages/C01a/task.md`（§1 角色、§8 工作项、§9 三段式契约、§10 不变量、§14 完成定义）
2. `docs/task-packages/C01a/evidence-manifest.md`（W01 盲隔离自证 + 各工作项证据）
3. `docs/task-packages/C01a/object-scope.md`（对象范围边界名单：12 操作 + 9 引用）
4. 候选正文 4 份：`D:\second-acceptance\candidates\C01a\` 下 `S01-sales-fulfillment.md`、`S02-sales-void.md`、`P01-purchase-receipt.md`、`P02-purchase-void.md`
5. 交叉核对用的实施侧结论：`docs/task-packages/B01–B05/` 各 `interface-facts.md`、`docs/ERPNext-MCP改造PRD.md`

## 验收项（逐条判定）

1. **盲隔离**：W01 证据可复核——`whoami=c01a-blind` + 全严禁路径 ACCESS_DENIED（不是「承诺不读」）。
2. **三段式完备**：每条候选含「初始数据状态 / 自然语言指令 / 期望终态断言骨架」，断言为字段级可判定（明确哪些记录的哪些字段变成什么），非自然语言主观描述。
3. **对象范围**：候选对象全部落在 object-scope.md 的 12 操作 + 9 引用名单内。
4. **S04 零痕迹**：候选无 B01–B05 接口结论、tool 名、档位词（全自动/人确认/只出 plan）、接口路径、MCP/server 实现痕迹、幂等/断言/回滚口径。**注意：这条要比盲会话的自扫更强——你现在手握 B01–B05 结论全文，逐条交叉核对。**
5. **落点**：候选仅在 `candidates/C01a/`（验收区隐藏），未进实施区。
6. **状态**：task.md §17 只到「待验收」，未自标「已通过」。

## 验收后记录（若全部通过）

- `task.md` §14 完成定义逐项勾选；§17 追加「待验收→已通过」「已通过→已封存」两行；新增 §18 封存记录与下游影响（对 C01b/D01/D02 的输入与门槛）。
- 登记表 C01a 行：状态 → 已封存，备注尾部「尚未实施」改为「已独立验收通过并封存（依据 task.md §18）」。
- 提交（commit message 带 Co-Authored-By 行）。

## 污染警告

你读完候选正文即受题污染，本会话是一次性验收会话——**之后不得参与 D/E/F 业务实现、实施方案评审、调参或定向修复**（总则 §2.5、C01a §1）。

## 退回路径

若任一项不满足（尤其 S04 发现 B 系列痕迹、三段式缺段、对象越界），**不标「已通过」**，出具退回记录（`docs/task-records/returns/`），列出具体不符项，交盲出题上下文重做。退回不污染结论、不改冻结题。
