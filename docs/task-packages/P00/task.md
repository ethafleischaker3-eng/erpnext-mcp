# P00 项目总控与治理基线

## 1. 基本信息

| 字段 | 内容 |
|---|---|
| 编号 | P00 |
| 名称 | 项目总控与治理基线 |
| 版本 | v1.1 |
| 状态 | 已通过 |
| 创建人 | Codex |
| Owner | gjg |
| Implementer | Codex（当前 P00 实施上下文） |
| Implementation Reviewer | gjg；不得读取后续对应隐藏验收材料 |
| Acceptance Reviewer | 不适用；P00 不接触正式验收材料 |
| Acceptor | gjg |
| 创建日期 | 2026-09-12 |
| 冻结日期 | 2026-09-12（v1.1）；v1.0 冻结日期为 2026-09-12 |
| 完成日期 | 2026-09-12 |
| 上游任务包 | 无；权威输入为冻结 PRD 与治理基线 |
| 下游任务包 | B00；其后为 A01 及登记表所列任务包 |

角色隔离说明：P00 不实施业务能力、不读取正式验收题；Acceptor gjg 不与 Implementer Codex 共用实施上下文。

## 2. 目标

建立项目可执行、可追踪且可回滚的总控基线，使后续任务能够明确识别当前权威版本、稳定编号、状态、责任人、依赖、写入边界、证据位置和阶段门槛；以 Git 固定治理与实现基线及 `frappe_docker` 依赖提交，并客观判断 B00 是否具备创建与冻结条件。

## 3. 非目标

- 不开发 MCP server 或任何正式业务 tool；
- 不调研或冻结 ERPNext API 行为；
- 不验证客户端 server 侧确认能力；
- 不生成、读取或运行正式验收题；
- 不建立正式业务权限、允许清单或幂等实现；
- 不以 P00 通过代替 B00、A01 或项目验收通过。
- 不创建远程仓库、不推送代码，也不配置远程凭据；
- 不改写 `frappe_docker` 的现有提交历史或工作树内容。
- 不把本地 Git、本地标签或同磁盘副本表述为灾难恢复能力。

## 4. 前置条件

| 条件 | 验证方式 | 状态 |
|---|---|---|
| ERPNext-MCP 改造 PRD 已冻结 | 检查 PRD 页头基线为 `2026-09-12-r1` | 已验证 |
| MCP 改造任务包总则 v1.0 已冻结 | 检查总则页头及 Freeze Manifest | 已验证 |
| 配套模板、登记表结构和边界规则已冻结 | 检查各文档页头及 Freeze Manifest | 已验证 |
| 治理记录目录允许在项目内建立 | 创建固定目录及说明文件并复核 | 已验证 |
| P00 Owner 已明确 | Owner 为 gjg | 已验证 |

## 5. 权威输入

| 优先级 | 名称 | 路径/位置 | 版本或提交标识 |
|---|---|---|---|
| 1 | 开源后端 Agent 化接入规范 | `docs/开源后端Agent化接入规范.md` | 2026-09-07 定稿版 |
| 2 | ERPNext-MCP 改造 PRD | `docs/ERPNext-MCP改造PRD.md` | 2026-09-12-r1 |
| 3 | MCP 改造任务包总则 | `docs/MCP改造任务包总则.md` | v1.0；以对应 Freeze Manifest 为准 |
| 4 | MCP 改造封闭任务包模板 | `docs/任务包模板.md` | v1.0；以对应 Freeze Manifest 为准 |
| 5 | MCP 改造任务包登记表 | `docs/任务包登记表.md` | v1.0 字段结构基线；任务行受控更新 |
| 6 | 实施区与验收区边界 | `docs/实施区与验收区边界.md` | v1.0 边界规则 |
| 7 | 治理修订评审记录 | `docs/变更记录-2026-09-12-任务包治理修订.md` | 2026-09-12 评审记录 |

## 6. 授权范围与所有权

### 6.1 可读取范围

- `docs/` 中的规范、PRD、治理文件及公开记录；
- 项目根目录的文件清单，仅用于核对治理路径；
- 不读取任何正式验收题、隐藏数据、终态断言或评分脚本。

### 6.2 写入集

| 路径/对象 | 允许动作 | Owner | 是否共享 | 协调规则 |
|---|---|---|---|---|
| `docs/task-packages/P00/` | 新增和维护 P00 任务及公开证据 | gjg | 否 | P00 状态变化必须追加记录，不覆盖历史 |
| `docs/任务包登记表.md` 的 P00 行 | 更新 P00 版本、状态、角色与位置 | gjg | 是 | 仅更新 P00 行；其他任务行实质变化另走相应任务或变更流程 |
| `docs/task-records/changes/` | 建立目录说明；后续保存任务包变更记录 | gjg | 是 | 每个变更使用稳定编号和独立文件 |
| `docs/task-records/returns/` | 建立目录说明；后续保存退回记录 | gjg | 是 | 每次退回使用稳定编号和独立文件 |
| `D:\second\.git/` | 初始化并维护项目主仓库元数据 | gjg | 否 | 仅在 P00 v1.1 冻结并进入实施中后创建；禁止改写已发布历史 |
| `D:\second\.gitignore` | 新建并维护根级忽略规则 | gjg | 是 | 必须覆盖环境文件、站点数据、日志、备份、缓存和依赖目录 |
| `D:\second\.gitmodules` | 登记 `frappe_docker` submodule | gjg | 是 | URL 固定为本地已配置值 `https://github.com/frappe/frappe_docker.git`；路径固定为 `frappe_docker`；不据此声称远端可达 |
| `D:\second\frappe_docker/.git` | 只读核查并将现有干净仓库登记为 submodule | frappe_docker 上游仓库 | 是 | 不修改、迁移或吸收其 `.git`；禁止运行 `git submodule absorbgitdirs`；固定提交 `a0c52135d4d41c4b8acf7adfdfc5bbcba46dd4d0` |

### 6.3 系统、接口、数据与环境权限

| 权限类别 | 允许范围/对象 | 允许动作 | 明确禁止 | 是否可改变状态 | 不适用理由/审批与证据 |
|---|---|---|---|---|---|
| 系统、接口与命令 | 本地文件系统、Git 本地命令及只读校验命令 | 创建治理文件、初始化主仓库、登记 submodule、创建本地提交和标签、列目录、计算哈希、文本检查 | 禁止调用 ERPNext 业务写接口；禁止 push、force push、改写历史和配置远程凭据 | 是，仅治理文件与本地 Git 元数据 | 用户要求将 Git 正式纳入 P00；操作记入 Evidence Manifest |
| 数据与凭证类型 | 公开治理文档 | 读取和引用 | 不得读取、记录或输出凭据值 | 否 | P00 不需要凭据 |
| 数据库状态 | 不适用 | 不适用 | 禁止连接或改变数据库 | 否 | P00 为纯治理任务 |
| 依赖安装 | 不适用 | 禁止 | 禁止安装或升级依赖 | 否 | P00 无依赖安装需求 |
| 运行环境与配置 | 项目内治理目录及 Git 元数据 | 新建目录说明、`.gitignore`、`.gitmodules` 和本地仓库 | 禁止启动、停止或修改业务运行环境 | 是，仅文件结构和 Git 元数据 | 由本任务交付物与 Git 证据证明 |
| 外部网络与服务 | 不适用 | 禁止 | 禁止外部调用及远程可达性探测 | 否 | P00 可完全离线完成；只核对已配置的远程 URL，不声称 GitHub 可访问 |

## 7. 禁止事项

- 不得修改上游规范、冻结 PRD、冻结治理规则和冻结验收题；
- 不得扩大对象、tool 或权限范围；
- 不得新增通用 CRUD 或任意代码执行面；
- 不得绕过 server 侧确认；
- 不得让失败路径静默成功；
- 不得输出密钥、密码、完整认证头、完整堆栈或未授权业务数据；
- 不得为通过验收而改变冻结题目；
- 不得将隐藏验收题、初始数据、评分细节或终态断言反馈给实施侧；
- 读取过隐藏验收材料的人员、会话或执行实例不得参与对应实现、实施方案评审、调参或定向修复；
- 不得将 P00 状态误作 B00、A01、L3 或项目完成状态；
- 不得在 P00 中创建正式验收材料。
- 不得将 `.env`、站点数据、数据库转储、备份、日志、凭据或缓存加入 Git；
- 不得把现有 `frappe_docker` 当作无 `.gitmodules` 的普通嵌套仓库提交；
- 未经另行明确授权不得向任何远程仓库推送。
- 不得运行 `git submodule absorbgitdirs` 或其他会迁移、删除、重建 `frappe_docker/.git` 的命令；
- 未配置异盘离线备份或远程仓库前，不得宣称具备磁盘损坏场景下的灾难恢复能力。

## 8. 工作项

| 编号 | 工作项 | 交付物/完成断言 |
|---|---|---|
| W01 | 登记项目权威基线 | 本文件列出精确版本，且可通过文档冻结页头、评审记录或适用的 Freeze Manifest 追溯 |
| W02 | 建立总控任务包与证据位置 | `docs/task-packages/P00/task.md` 和 `evidence-manifest.md` 存在 |
| W03 | 建立治理记录固定目录 | `docs/task-records/changes/` 与 `returns/` 已建立并含使用说明 |
| W04 | 同步登记表 P00 行 | 版本、状态、角色、路径和证据位置与本文件一致 |
| W05 | 固定阶段门槛和下一任务 | 明确 B00 为下一任务，B00 通过后方可创建并冻结 A01 |
| W06 | 登记存量证据接管原则 | B00 必须重新验证或书面采纳治理生效前探索证据并生成新证据编号 |
| W07 | 登记隔离边界待办 | 实际路径、Owner 和访问验证未完成前，阻断 C01a/C01b/G01 等验收材料任务 |
| W08 | 建立项目 Git 主仓库 | `D:\second` 成为主仓库，默认分支和本地身份配置已记录，且不存在意外纳入的敏感/运行文件 |
| W09 | 建立根级忽略规则 | `.gitignore` 覆盖 `.env`、sites、日志、备份、转储、缓存、虚拟环境和依赖目录，并通过 `git check-ignore` 验证代表样例 |
| W10 | 固定 frappe_docker 依赖 | 以 submodule 登记 `https://github.com/frappe/frappe_docker.git`，固定提交 `a0c52135d4d41c4b8acf7adfdfc5bbcba46dd4d0`，不改变其干净工作树 |
| W11 | 创建可回滚治理基线 | 提交 PRD、治理基线及冻结的 P00 定义，并在该提交创建本地注释标签 `governance-p00-v1.1`；标签提交不得因后续证据收尾而移动 |
| W12 | 完成证据收尾并验证回滚路径 | S08 先检查证据收尾提交的父提交或待提交候选状态；提交 S08 证据和 Manifest 更新后，由 Acceptor 对最终 HEAD 做不改变工作树的只读复核。标签后仅允许证据、状态记录、Freeze Manifest 和回滚说明变化；记录后续优先使用 `git revert` 的恢复规则 |

## 9. 输入输出契约

不适用。P00 只交付治理文档和目录，不提供 tool 或业务接口。

## 10. 不变量

- 冻结 PRD、总则、模板及边界规则内容不得被 P00 修改；
- 任务包编号不得重排或复用；
- 登记表动态任务行更新不得改变其冻结字段结构和状态枚举；
- 未满足上游门槛的任务不得进入实施中；
- 正式验收材料不得进入实施区或 P00 公开证据；
- 治理生效前探索证据不得直接充当 B00 正式完成证据；
- P00 不得改变 ERPNext 数据库或业务运行环境。
- Git 暂存区与提交不得包含根级忽略规则禁止的文件；
- `frappe_docker` 必须以 gitlink/submodule 形式固定到指定提交，不得作为普通目录递归纳入；
- P00 不得执行远程 push 或任何历史改写操作；
- 基线提交和标签必须可解析到同一已记录提交。
- `governance-p00-v1.1` 必须固定治理基线提交且不得移动；当前 HEAD 可以是其后代，但标签后的提交只能修改证据、状态记录、Freeze Manifest、登记表动态状态及回滚说明；
- 本地 Git 只能支持误改回退；没有异盘副本或远程备份时，不构成磁盘损坏、丢失或勒索场景下的灾难恢复。

## 11. 交付物

| 编号 | 交付物 | 存放位置 | 验收方式 |
|---|---|---|---|
| D01 | P00 任务包 | `docs/task-packages/P00/task.md` | 按冻结模板检查必备结构 |
| D02 | P00 Evidence Manifest | `docs/task-packages/P00/evidence-manifest.md` | 逐项追溯工作项与完成定义 |
| D03 | 更新后的任务包登记表 | `docs/任务包登记表.md` | P00 行与本文件一致，表格结构未改变 |
| D04 | 变更记录目录说明 | `docs/task-records/changes/README.md` | 文件存在并明确编号及模板规则 |
| D05 | 退回记录目录说明 | `docs/task-records/returns/README.md` | 文件存在并明确编号及模板规则 |
| D06 | 根级 Git 忽略规则 | `.gitignore` | 静态检查并用 `git check-ignore` 验证代表样例 |
| D07 | submodule 登记 | `.gitmodules` 及 Git index 中的 gitlink | URL、路径和固定提交与冻结要求一致 |
| D08 | 项目治理基线提交与标签 | 本地主仓库基线提交及 `governance-p00-v1.1` | 标签可解析并固定指向治理基线提交；后续收尾提交不得移动标签 |
| D09 | Git 回滚说明 | `docs/task-packages/P00/git-baseline.md` | 包含仓库边界、依赖提交、基线标签及其提交 ID、允许的收尾范围和 `git revert` 原则；不要求记录会因收尾而变化的当前 HEAD，不包含凭据 |

## 12. 开发自检

| 编号 | 检查方法 | 预期结果 |
|---|---|---|
| S01 | 检查 D01-D05 文件是否存在 | 全部存在；D01/D02 的正式输出保存到 `evidence/S01-files-<时间戳>.txt`，D04/D05 的正式输出保存到 `evidence/S01-governance-dirs-<时间戳>.txt`，D03 由 S02 单独验证 |
| S02 | 检查登记表 Markdown 各行列数 | 所有表格行列数一致；正式输出保存到 `evidence/S02-registry-columns-<时间戳>.txt` |
| S03 | 重新计算不可变治理文件 SHA-256，并检查登记表字段结构与列数 | 不可变文件与 Freeze Manifest 一致；登记表动态行更新后仍保持冻结字段结构且各行列数一致；正式输出保存到 `evidence/S03-freeze-hashes-<时间戳>.txt` |
| S04 | 搜索 P00 文件中的未处理占位项 | 除冻结日期、完成日期和正式证据状态等阶段性字段外无未处理占位项；正式输出保存到 `evidence/S04-placeholders-<时间戳>.txt` |
| S05 | 检查本任务的数据库、外部网络和依赖安装操作记录 | 均未发生；由实施者声明及命令/工具调用记录共同证明，正式记录保存到 `evidence/S05-boundary-<时间戳>.md` |
| S06 | 检查 Git 跟踪与忽略结果 | 无敏感/运行文件被跟踪；代表样例命中预期忽略规则；正式输出保存到 `evidence/S06-git-ignore-<时间戳>.txt` |
| S07 | 检查 submodule | `.gitmodules`、gitlink、已配置远程 URL及提交均符合 W10，内部工作树干净且 `.git` 未被迁移；不进行 GitHub 可达性探测；正式输出保存到 `evidence/S07-submodule-<时间戳>.txt` |
| S08 | 检查基线标签及候选收尾范围 | 在提交 S08 证据前，检查证据收尾提交的父提交或待提交候选：基线标签为其祖先，标签后路径均属于允许范围。提交证据后由 Acceptor 对最终 HEAD 和全部冻结完成定义做只读复核；完整复核信息必须写入指向最终 HEAD 的 annotated tag `governance-p00-v1.1-accepted`，且不得修改工作树 |

## 13. Evidence Manifest

证据统一维护在 `docs/task-packages/P00/evidence-manifest.md`，本文件不重复记录明细。

## 14. 完成定义

- [x] 全部前置条件已经在冻结后重新验证；
- [x] 工作项与交付物全部完成；
- [x] 契约不适用，适用不变量均有正式证据；
- [x] 输入输出契约及冻结完成定义未被擅自改变；
- [x] 冻结后正式开发自检通过；
- [x] Evidence Manifest 完整且正式证据可独立复核；
- [x] 未修改禁止范围；
- [x] 剩余限制和风险已记录；
- [x] 独立验收通过。

## 15. 停止与升级条件

继承《MCP 改造任务包总则》第 6.15 节。另有以下 P00 条件：冻结治理文件哈希不一致、登记表结构意外变化、责任人无法确认或固定治理路径不可用时，停止推进 B00 并转为待澄清。

## 16. 风险、假设与待决事项

- 实施区实际路径暂定为项目工作区 `D:\second`，仍须在 B00 前明确允许范围并验证；
- 验收区实际路径、Owner 和访问控制尚未确定；在验证完成前不得启动任何接触正式验收材料的任务；
- 当前工作区不是 Git 仓库，冻结追溯暂以独立 SHA-256 Freeze Manifest 为准；后续若引入版本库，须在 P00 状态记录中登记提交标识；
- `frappe_docker` 当前是独立且干净的 Git 仓库，远程为 `https://github.com/frappe/frappe_docker.git`，固定候选提交为 `a0c52135d4d41c4b8acf7adfdfc5bbcba46dd4d0`；将其登记为 submodule 时必须保留工作树内容和提交，不得先行删除或重置；
- P00 禁止外部网络访问，因此上述 GitHub 地址仅为本地配置事实，不代表已验证远端可达、仓库仍存在或当前主体具有访问权限；
- 本地仓库和本地标签与项目文件位于同一磁盘，只能处理误改和逻辑回退；异盘离线备份或远程备份须另行授权和规划；
- P00 v1.0 在 Git 建立前被 v1.1 修订稿原位覆盖，现仅保留冻结哈希，未保留可验证的只读内容快照；该追溯缺口记录于 `CHG-P00-20260912-001`，不得声称 v1.0 内容仍可完整恢复；
- B00、A01 及后续任务的 Implementer、Reviewer 与 Acceptor 尚待逐包指定，不由 P00 代填；
- P00 当前仅完成冻结前草案和预检；必须先经 gjg 评审冻结，再进入“实施中”重新执行正式自检并生成证据，随后才能转为“待验收”。

## 17. 评审与状态记录

| 时间 | 原状态 | 新状态 | 操作人 | 依据/说明 |
|---|---|---|---|---|
| 2026-09-12 | 规划中 | 草拟 | Codex | 按冻结模板创建完整任务包并指定 Owner |
| 2026-09-12 | 草拟 | 待评审 | Codex | 必备结构和冻结前预检已准备，提交 gjg 评审；预检不构成正式实施或验收证据 |
| 2026-09-12 | 待评审 | 已冻结 | gjg | 评审通过，批准 P00 以 v1.0 冻结；完成定义仍待正式实施和验收 |
| 2026-09-12 | 规划中 | 草拟 | gjg | 启动 P00 v1.1 修订；v1.0 保持已冻结并由 v1.1 拟议版本取代，不执行未列明的跨版本状态跳转 |
| 2026-09-12 | 草拟 | 待评审 | Codex | 按 CHG-P00-20260912-001 完成 Git 主仓库、根级忽略规则、submodule、基线提交和回滚验证修订，提交 gjg 重新评审冻结 |
| 2026-09-12 | 待评审 | 已冻结 | gjg | 复核通过，无阻断问题；批准 P00 v1.1 冻结。EV-P00-012 须在最终验收时复核全部完成定义并写入 annotated acceptance tag |
| 2026-09-12 | 已冻结 | 实施中 | gjg | 依据 P00 v1.1 Freeze Manifest 进入实施中；仅动态状态更新，不改变冻结目标、范围与完成定义 |
| 2026-09-12 | 实施中 | 待验收 | Codex | 完成 W01—W12 及 S01—S08 正式自检，生成 EV-P00-001 至 EV-P00-011 证据并提交全部交付物；待 Acceptor 独立验收（EV-P00-012） |
| 2026-09-12 | 待验收 | 已通过 | gjg | 独立验收通过（EV-P00-012）：全部完成定义成立，证据可独立复核；annotated tag governance-p00-v1.1-accepted 指向最终 HEAD |
