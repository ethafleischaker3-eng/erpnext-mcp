# E02 独立验收记录（CD-6）

> 文档性质：E02 v1.0 档位 2（标准）独立验收记录。Acceptor=gjg，独立于 Implementer（Claude，E02 独立实施上下文）。
> 验收方式：不依赖正式验收题或隐藏材料；以「隔离探针 + 单元测试」构成可复算断言，Acceptor 在独立会话中逐项复核机制实现、实现契约与证据，并独立重跑自检（不接受实施侧自证）。

## 1. 验收基本信息

| 字段 | 内容 |
|---|---|
| 任务包 | E02 幂等机制、前置断言与事后校验、批次台账与状态查询 |
| 版本 | v1.0 |
| 档位 | 档 2（标准）；终态「已通过」，不单独封存（封存合并入 P00 或直接下游） |
| Acceptor | gjg（独立于 Implementer） |
| Acceptance Reviewer | 不适用（E02 不接触正式验收题、隐藏数据或终态断言） |
| 验收日期 | 2026-09-16 |
| 验收结论 | **通过** |

## 2. 验收依据

- 冻结定义：`docs/task-packages/E02/task.md` §9.2（档位 2 独立验收口径）与 §14（完成定义）；
- 冻结快照：`docs/task-packages/E02/frozen/v1.0/task.md`（SHA-256 `ca16b976…`）与 `frozen/v1.0/evidence-manifest.md`（SHA-256 `1ca2932c…`），与 `docs/task-records/freeze-manifests/E02-v1.0.md` 一致；
- 权威输入：B05 §18.3（幂等边界）、B00 §18.3 第 1 条（可信调用方身份）、规范 §9.3–§9.6、PRD §5.1–§5.4、D01 common-contract.md（共同对齐参考）。

## 3. 独立复核结果

### 3.1 冻结完整性

- 冻结快照哈希与 Freeze Manifest 一致（`ca16b976…` / `1ca2932c…`），当前 `task.md` 仅在冻结语义之上追加状态记录与风险条目，未覆盖冻结内容（快照 `frozen/v1.0/` 未动）。
- 工作区改动面核对：仅新增 `docs/task-packages/E02/`（实现 + 探针 + 测试 + 证据）与 `docs/task-records/freeze-manifests/E02-v1.0.md`、修改 `docs/governance/任务包登记表.md` 的 E02 行。未改动规范、PRD、总则、模板、边界规则、B00/B05/D01 冻结材料、`frozen/` 快照。

### 3.2 开发自检（独立重跑，非采信实施侧自证）

| 检查 | 独立重跑结果 |
|---|---|
| `test/selftest.js`（S01—S09，45 项） | 45 通过 / 0 失败，退出码 0 |
| `probe/selftest.js`（P01—P06，6 项） | 6 通过 / 0 失败，退出码 0 |

Acceptor 另以独立脚本抽查边界：顶层传输字段剥离、嵌套行项目业务字段保留（键序不同仍同指纹）、窗口期 300/60 边界（300 允 / 301 拒、60 允 / 61 拒）、未知状态回滚路径返回 `null`（不猜测）——均符合冻结口径。

### 3.3 §9.2 四项验收口径逐项判定

| 口径 | 判定 | 证据 |
|---|---|---|
| 幂等：同参（含键序不同）同指纹、二次命中返回合并；异载异指纹；传输/客户端字段注入指纹不变；窗口期命中/超期可复算 | 通过 | S01.1–S01.4、S02.1–S02.6、S03.1–S03.2 |
| confirm/cancel 状态断言：模拟「已在目标状态」→ 幂等成功语义，非通用错误 | 通过 | S04.1、S04.2、S04.4、S04.5（`isError:false` + `idempotent_replay:true`，无 `code` 字段） |
| 前置/事后校验框架：断言失败/回读不一致 → 对应可自纠报错并记批次、标记待回滚 | 通过 | S05.1–S05.2、S06.2–S06.4 |
| 批次台账：批次标识、同批变更、回滚路径、归属可见性可复算；普通调用方不可见他人批次、管理员可查全量 | 通过 | S07.1–S07.8、S08.1–S08.6 |

### 3.4 实现契约（CD-5）覆盖核对

- `implementation-contract.md` §1–§7 与 task.md §9.1 第 1–7 项一一对应，S09.1 通过；
- 全文扫描 #1–#25 业务 tool 名零命中（S09.2），#26 仅以查询语义引用、不冻结 name/schema/annotation（S09.3）——未写死任何业务 tool 契约；
- 幂等边界忠实 B05 §18.3：窗口期 create 300s / confirm·cancel 60s 未放宽（`assertWindowNotWidened` 拒绝超值）、指纹 server 规范化 + 传输字段剥离、confirm/cancel 状态断言幂等成功；与 D01 common-contract 重叠项以 D01 口径为准（§0.4）。
- 批次归属采用会话级身份（B00 §18.3 + B05 §3 确定性结论），跨会话普通调用方查询旧批次不支持并如实记录为残余限制（契约 §5.5、§8.2），未宣称未成立能力。

### 3.5 Evidence Manifest

`evidence-manifest.md` 已回填 EV-E02-001–009，逐项映射 W01–W09 / S01–S09 并给出原始输出位置（`evidence/selftest-S01-S09.txt`、`evidence/probe-selftest.txt`），与本次独立重跑输出一致；无敏感凭据、无后端原始数据。

## 4. 完成定义（§14）逐项判定

| 完成定义 | 判定 |
|---|---|
| 全部前置条件已在冻结后重新验证 | 通过（B05、B00 均已封存，登记表行与 Freeze Manifest 核对） |
| 工作项与交付物全部完成 | 通过（W01–W09；CD-1–CD-6） |
| 实现契约输出结构与完成定义未被擅自改变 | 通过（覆盖 §9.1 七项，冻结快照未动） |
| 适用不变量均有正式证据 | 通过（S01–S08 覆盖幂等/断言/校验/批次/可见性不变量） |
| 冻结后正式开发自检通过 | 通过（45/45 + 6/6，独立重跑） |
| Evidence Manifest 完整且正式证据可独立复核 | 通过 |
| 未修改禁止范围 | 通过（工作区改动面核对） |
| 剩余限制和风险已记录 | 通过（契约 §8、task.md §16） |
| 独立验收通过（档位 2 强制） | 通过（本记录） |

## 5. 发现项（非阻断）

- **S04.3 断言为空操作**：`test/selftest.js` S04.3 的表达式 `resp.code !== 'precondition_failed' && !/失败|错误/.test(resp.message) || true` 因运算符优先级恒为 `true`，未实际校验「非 precondition_failed 且消息不含失败/错误措辞」。该语义已由 S04.2（`isError===false && idempotent_replay===true && already_in_target_state===true`）实质覆盖，机制本身行为正确（返回对象无 `code` 字段、`isError:false`）。建议后续将 S04.3 收紧为不含 `|| true` 的确定性断言。**不构成验收阻断。**

## 6. 结论

E02 v1.0 机制实现（`lib/` 8 模块）、实现契约（覆盖 §9.1 全部 7 项）、隔离探针（3 探针 tool）与单元测试（S01—S09）齐备，§9.2 四项验收口径与 §14 完成定义全部满足，未修改禁止范围、未读取 C01a、未接入 ERPNext 后端。**Acceptor（gjg）独立验收通过。**

> 后续（2026-09-16）：档位 2 本「不强制单独封存」，Owner 决策执行独立封存，并收紧 S04.3 断言（移除恒真 `|| true`，机制行为不变，复跑 45/45 通过）。封存与下游影响记录见 `task.md` §18。
