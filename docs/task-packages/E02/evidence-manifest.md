# E02 Evidence Manifest

> 当前状态：E02 v1.0 已**实施完成并自检通过（2026-09-16）、gjg 独立验收通过并封存（档位 2，2026-09-16，见 `acceptance-record.md` 与 task.md §18）**。本清单为证据记录（已回填实际值）。下列 `EV-E02-*` 为冻结自检方法 S01—S09 的证据映射，逐项执行并回填了实际值、原始输出位置与状态。本包为档位 2（标准）公共安全底座实施包：无后端交互、无数据库写入，证据形式为机制实现（server 侧模块/函数 + 隔离探针）的单元测试/探针运行记录与实现契约（`implementation-contract.md`）的逐项交叉核对记录。

## 1. 证据映射（已回填）

| 证据编号 | 对应工作项/自检 | 计划操作 | 预期值 | 原始输出位置 | 状态 |
|---|---|---|---|---|---|
| EV-E02-001 | W01、S01 幂等指纹引擎 | 指纹函数单测：同参（键序不同）、异载、注入传输/客户端字段 | 同参同指纹、异载异指纹、注入传输字段指纹不变 | `evidence/selftest-S01-S09.txt`（S01 段，4 项） | 已完成 |
| EV-E02-002 | W02、S02 窗口期管理 | 窗口期单测：窗口期内命中、超期不命中 | create 300s / confirm·cancel 60s 未放宽 | `evidence/selftest-S01-S09.txt`（S02 段，6 项） | 已完成 |
| EV-E02-003 | W03、S03 误合并策略与业务引用号 | 同参命中返回合并、业务引用号接入点存在 | 合并语义正确、引用号边界留 D02 | `evidence/selftest-S01-S09.txt`（S03 段，3 项） | 已完成 |
| EV-E02-004 | W04、S04 confirm/cancel 状态断言兜底 | 模拟「已在目标状态」→ 返回幂等成功 | 返回幂等成功语义，非通用错误 | `evidence/selftest-S01-S09.txt`（S04 段，5 项） | 已完成 |
| EV-E02-005 | W05、S05 前置断言框架 | 模拟 schema 覆盖与业务状态挂钩、失败返回 | 两层拆分清晰、完成 schema 校验≠满足前置断言 | `evidence/selftest-S01-S09.txt`（S05 段，5 项） | 已完成 |
| EV-E02-006 | W06、S06 事后校验框架 | 模拟回读不一致→报错+记批次+标记待回滚 | 两层拆分清晰、返回值符合 schema≠写入成功 | `evidence/selftest-S01-S09.txt`（S06 段，5 项） | 已完成 |
| EV-E02-007 | W07、S07 批次台账 | 多批次标识/同批变更/回滚路径/归属可见性 | 普通调用方不可见他人批次、管理员可查全量 | `evidence/selftest-S01-S09.txt`（S07 段，8 项） | 已完成 |
| EV-E02-008 | W08、S08 #26 查询逻辑 | 归属自身批次可查、他人不可见、不可主动回滚 | 可见性边界正确 | `evidence/selftest-S01-S09.txt`（S08 段，6 项） | 已完成 |
| EV-E02-009 | W09、S09 实现契约覆盖 | 核对 `implementation-contract.md` 覆盖 §9.1 全部 7 项、全文扫描不含具体 tool 语义 | 无 #1–#25 逐 tool 的 name/description/schema/断言 | `evidence/selftest-S01-S09.txt`（S09 段，3 项） | 已完成 |

补充证据：探针可运行性自测 `evidence/probe-selftest.txt`（P01—P06 全部通过，退出码 0），证明机制可作为 stdio MCP server 进程运行并可注入合成故障（`postcondition_mismatch`）。

## 2. 正式证据记录要求

执行者=Claude（E02 独立实施上下文）、时间=2026-09-16（UTC+8）、环境=本机只读文件访问 + 隔离探针/单元测试（Node.js v24.19.0，免外部依赖）。本包为档位 2 公共安全底座，证据为机制实现的可复算测试记录与实现契约交叉核对记录：每项证据给出「上游权威文件条款 → 实现契约/机制实现条款」的逐条对应（见 `implementation-contract.md` §9 对照索引）与可复算测试输出（退出码/断言通过/失败），非「已覆盖」等主观表述。S09 已附全文扫描命令（`test/selftest.js` S09 段对 `implementation-contract.md` 全文扫描 #1–#25 业务 tool 名零命中）与「零命中」输出。本包不接入 ERPNext、不涉数据库，无敏感后端原始输出需转存验收区。

## 3. 敏感信息与受控位置

- E02 不接入 ERPNext 后端、不调用其 REST API 或数据库，无后端凭据进入本包；
- 本包不读取 C01a 隐藏候选（`D:\second-acceptance\candidates\C01a\`）或任何正式验收材料；
- 机制实现与实现契约落实施区，不含密码、密钥、认证头或完整堆栈；B05 窗口期/指纹结论与 B00 身份结论摘要（共用 Administrator）为公开事实，非敏感。
